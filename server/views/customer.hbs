{{> header}}
{{> menu}}
<main>
    <h1>Llistat de Clients i Lloguers</h1>
    <div class="grid">
        {{#each customers}}
            <div class="card">
                <h3>{{this.first_name}} {{this.last_name}}</h3>
                <h4>Últims lloguers:</h4>
                <ul>
                    {{#each this.rentals}}
                        <li>{{this.title}} - {{this.rental_date}}</li>
                    {{/each}}
                </ul>
            </div>
        {{/each}}
    </div>
</main>
{{> footer}}